from imports import *
import flet as ft

#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
os.path.exists('path') # вернет true, если файл в директории есть
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
x = "[1,2,3,4]"
literal_eval(x) # вернет нормальный массив из строчного массива
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
scroll=ft.ScrollMode.ALWAYS # делает column или row скролом
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
text = 'a,b,c'
text = text.split(',') # преобразовывает строку в массив через сепаратор
#-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
# массив в строку
lst = ['1','2','3']
' '.join(lst)































